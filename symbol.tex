% NAND gate - standard ANSI/IEEE distinctive-shape symbol.
% An AND shape with a bubble on the output (bubble = inversion).
\documentclass[border=12pt]{standalone}
\usepackage{circuitikz}
\begin{document}
\begin{circuitikz}[line width=1.1pt, scale=1.4, transform shape]
  \ctikzset{tripoles/thickness=1, bipoles/thickness=1, logic ports/thickness=1}
  \node[nand port] (g) {};
  \draw (g.in 1) -- ++(-1,0) node[left]{$A$};
  \draw (g.in 2) -- ++(-1,0) node[left]{$B$};
  \draw (g.out)  -- ++(1,0)  node[ocirc]{} node[right]{$Y=\overline{A\cdot B}$};
\end{circuitikz}
\end{document}
